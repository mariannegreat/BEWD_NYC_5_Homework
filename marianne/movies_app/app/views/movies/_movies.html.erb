<% @movies.each do |movie| %>
	<p><b><%= movie["title"] %></b> (<%= movie["year_released"] %>)<br/>
	<em><%= movie["description"] %></em><br/>
	Rating: <%= movie["rating"] %>/5<br/>
	<%= link_to "Show", movie_path(movie) %> | <%= link_to "Edit", edit_movie_path(movie) %></p>
<% end %>